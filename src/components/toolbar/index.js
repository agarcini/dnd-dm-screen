import { h } from "preact"
import styled from "styled-components"

const ToolbarDiv = styled.div`
	padding: 60px 2% 10px;
	width: 33%;
`

const Toolbar = () => <ToolbarDiv></ToolbarDiv>

export default Toolbar
